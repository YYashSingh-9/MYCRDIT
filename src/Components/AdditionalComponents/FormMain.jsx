import { Box } from "@mui/material";
import classes from "./FormMain.jsx";
import { Form } from "react-router-dom";

const FormMain = () => {
  return (
    <>
      <Box>
        <Form method="POST"></Form>
      </Box>
    </>
  );
};

export default FormMain;
